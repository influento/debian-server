
  Custom Debian Server Installer ISO

  To start the installer:
    bash /root/debian-install/install.sh

  For options:
    bash /root/debian-install/install.sh --help

  VM testing (server):
    bash /root/debian-install/install.sh --config /root/debian-install/tests/vm-server.conf

