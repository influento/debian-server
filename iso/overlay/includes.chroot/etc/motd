
  Custom Debian Server Installer ISO

  To start the installer:
    bash /root/debian-install/start.sh

  For options:
    bash /root/debian-install/install.sh --help

  SSH into this live ISO (root:root):
    ssh -p 2223 root@<host-ip>

